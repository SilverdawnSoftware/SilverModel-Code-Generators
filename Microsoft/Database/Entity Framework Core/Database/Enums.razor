@using SilvaDawn.SilvaModel.UML.Extensions;
@using SilvaDawn.SilvaModel.UML.Interfaces.Classes;
@using SilvaDawn.SilvaModel.UML.Classes;
// ALLOWOVERWRITE

using System;
using System.Collections.Generic;
using System.Linq;



namespace @(Model.Diagram.Name).Database
{
    public enum @Model.Enumeration.Name
    {
    	@foreach (var att in Model.Enumeration.EnumerationLiterals())
    	{
    		
    		if (att.Value!=null)
    		{
    		@att.Name@:= @att.Value
    		}
    		else
    		{
    		@att.Name
    		}
    		@:,
    		
    	}
    	
    }
    
    
}