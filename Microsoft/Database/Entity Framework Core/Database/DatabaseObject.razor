@using SilvaDawn.SilvaModel.UML.Extensions;
@using SilvaDawn.SilvaModel.UML.Interfaces.Classes;
@using SilvaDawn.SilvaModel.UML.Classes;
// ALLOWOVERWRITE

using System;
using System.Collections.Generic;
using System.Linq;



namespace @(Model.Diagram.Name).Database
{
    public class @Model.Classifier.Name
    {
    	@foreach (var att in Model.Classifier.Attributes()) { 
    	switch (att.Type.GetUMLType())
				{
				case UmlTypeEnum.PrimitiveType:			
	    		<text>
	    		/// <summary> @att.Description </summary>	 
	    		
	    		public @att.Type.TypeName("C#")</text> @att.Name <text> { get; set; } </text>    	
				break;
				case UmlTypeEnum.Class:				
				<text>
				/// <summary>
                /// @att.Description
                /// </summary>	
				public virtual @att.Type.TypeName("C#")</text> @att.Name <text> { get; set; } </text>    				
				break;
				}
    	
    	
    		
    	}
    	
    	
    	@foreach (var att in Model.Classifier.GetTargetAssociation()) { 
    	<text>
    	/// <summary>
        /// Reference to  @att.GetSourceMemberEnd().Type.Name
        /// </summary>	
    	public virtual @att.GetSourceMemberEnd().Type.Name</text> @att.GetSourceMemberEnd().Type.Name<text> { get; set; } </text>
    	}
    	
    	@foreach (var att in Model.Classifier.GetSourceAssociation()) { 
    	<text>
    	/// <summary>
        /// List of @(att.GetTargetMemberEnd().Type.Name)s
        /// </summary>	
    	public virtual List<@att.GetTargetMemberEnd().Type.Name></text> @att.GetTargetMemberEnd().Type.Name<text>s { get; set; } </text>
    	}
    	
    	
    	
    	
    }
    
    
}



























