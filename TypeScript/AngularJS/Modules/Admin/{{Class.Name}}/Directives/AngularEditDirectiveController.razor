@using SilvaDawn.SilvaModel.UML.Extensions;
@using SilvaDawn.SilvaModel.UML.Interfaces.Classes;
@using SilvaDawn.SilvaModel.UML.Classes;
// ALLOWOVERWRITE

module @Model.Diagram.Name {


    export interface IButtonClick {
    };

    export interface ITextChange {
    };


    export interface I@(Model.Classifier.Name)EditDirectiveCtrlScope extends ng.IScope {

        loaded: boolean;
        component: any;
    }


    export class @(Model.Classifier.Name)EditDirectiveCtrl {
        public static $inject = [
            '$scope',
            'usSpinnerService',
            '@(Model.Classifier.Name.LowerFirst())Service'
        ];

		constructor(private $scope: I@(Model.Classifier.Name)EditDirectiveCtrlScope, private usSpinnerService: any,private @(Model.Classifier.Name.LowerFirst())Service: @(Model.Classifier.Name)Service) {
			
			
			
			
		}
		
	}
	angular.module("@Model.Diagram.Name").controller('@(Model.Classifier.Name.LowerFirst())EditDirectiveCtrl', @(Model.Classifier.Name)EditDirectiveCtrl);	
}